# Known divergences (by design): compute_exection_time() lives in schema testc and
# runs a dynamic EXECUTE the gateway rejects; it is re-seeded (into schema testc)
# before every test so the test runs. Because the seed pre-creates schema testc,
# the test's own CREATE SCHEMA testc reports "already exists" -- a one-line
# seeding artifact.
--- a
+++ b
@@ -1,3 +1,5 @@
+ERROR: schema "testc" already exists
+ERROR: EXECUTE of a runtime-built statement inside a PL/pgSQL body is not supported: the statement text is not a constant, so it cannot be checked for unsafe session-state changes
 t
 t
 NOTICE: drop cascades to 4 other objects
